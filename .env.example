# Phase 1: not consumed yet. Wired up when Supabase integration lands.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Phase 2: optional. Without this, MapView renders a stylized mock map
# instead of live Mapbox tiles — the app never shows a broken map.
VITE_MAPBOX_TOKEN=
